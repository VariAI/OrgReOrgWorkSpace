# 2026-07-24

## 企微作为组织与用户来源:调研毕,暂缓

- 用户提出服务端对接企微作组织/用户来源 → 定性:上游数据 SoR,与「自有 IM 取代企微(入口)」不冲突;与身份统一缝强相关(person-id 种子=企微 userid,Server 缺通用外部身份映射表)。
- Server 侧核实:organization 模块可直接承接;同步落库应走 Service 层(群投影+organizationUpdate CMD 自动触发,满足数据新鲜度约定);定时设施现成(timingwheel)。
- 企微 API 官方文档核实(修正首轮误判):**自建应用 secret 拿得到姓名**(2022-06-20 砍的是手机/邮箱/头像等,姓名不在列);「通讯录同步 secret」2022-08-15 起只剩 ID 不可用;手机号/邮箱拿不到 → 同步员工登录凭证问题(默认密码 or 扫码登录,待拍)。
- 用户拍板:①同步方式=每次全量对账(不做回调) ②**先不做**。立 `plans/2026-07-24-wecom-directory-source.md`(deferred,调研留存),backlog 挂暂缓。

## 管理端整轮打磨 + 手机号即登录账号 + 导入 400 人实测

- 管理端信息架构收敛(Admin d19b3f8/f4f55ac 等):组织菜单二级化、组织页全能化(员工日常管理合并+页内员工抽屉+新增员工入口)、账号域三合一(两标签+封禁筛选)、管理员授权入口移除(服务端休眠,组织管理仅超管)、15 张过时截图删除。
- **手机号即登录账号**(用户拍板;Server 561c0ff + Admin bad02c1):服务层统一派生 username=区号+手机号(建/改/绑定三路径,改手机号自动跟随),CSV 登录账号列降为可选,Admin 表单删登录账号字段。存量脏账号(86xxx)修复路径=编辑资料保存一次。
- 生成导入模板(根目录 组织员工导入模板.csv/.xlsx)+ 400 人公司测试数据(6 中心 18 组/含兼任离职样本)。
- 400 行实测抓到真 bug:预检整包 JSON 撑爆 org_import.preview_summary(TEXT 64KB,Error 1406)。修复=摘要瘦身(计数+缺失部门+至多 50 条错误行;明细行本就单独存 org_import_row,commit 只读那份)。待提交。
- 立 backlog P3:预检 400 行 6.7s(逐行 3 次查库≈1200+ 次),改三键 IN 批量查。

## 桌面端标题栏一体化(Win/Mac,OrgReOrgWeb)

- 用户反馈 Win 版原生标题栏与内容有分离感 → 改为 `titleBarStyle: hidden`(mac `hiddenInset` 保留红绿灯) + Windows WCO `titleBarOverlay`(配色对齐 `--wk-canvas` 亮/暗两套,主题切换经 IPC `titlebar-theme-changed` 调 `setTitleBarOverlay` 同步)。
- 渲染层 App 外壳加 32px 画布色拖拽带(`-webkit-app-region: drag`,仅 `__POWERED_ELECTRON__` 时渲染,浏览器/Tauri 不受影响);Windows 菜单栏彻底移除(Alt 弹出菜单破坏一体感,编辑类快捷键 Chromium 原生支持)。
- Tauri 壳(v1.5)不动:Windows 侧无 WCO 等价能力,要一体化须 `decorations:false`+自绘窗口按钮,属独立任务;当前发行链是 Electron。
- 验证:渲染层 tsc 基线 4 错误无新增;主进程 `tsconfig.e.json` 零错误;`yarn build` 通过。待真机验收:拖拽/双击最大化/暗色切换后右上角按钮区配色。未提交 git。
