# 2026-07-23

## 建立根治理层 AGE 最小骨架

- 读 4 个子仓 README/AGENTS,确认:产品 = 企业级上下文管理,IM 为载入口,用户 = 企业员工;root 层职责 = 治理 4 仓,不改子仓内部。
- 建:根 `AGENTS.md` / `CLAUDE.md`、`docs/index.md`、`docs/context/{project-context,ai-autonomy-policy,codebase-map}.md`、`docs/architecture/system-baseline.md`、`docs/backlog/README.md`。
- 记录开放问题:OQ-1(引擎↔IM 集成缝未记录)、OQ-2(整体部署拓扑)、OQ-3(差异化定位)。
- 验证:本层为文档,人工核链接与一致性;子仓验证命令见 `project-context.md`,未在本层执行。
- 待用户:OQ-1、OQ-3。

## OQ-1 / OQ-3 结案 + 第一根缝立项

- 用户拍板:产品 = 「agent 里的 IM」(非「IM 里的 agent」,区别于 openclaw/Hermes);集成深度 **L2 为目标、L1 只作跳板**;自有 IM 为**唯一**人机入口,取代 Matrix/Element/企微。
- 建 `architecture/project-vision.md`(OQ-3 结案);`system-baseline.md` OQ-1 结案为集成契约(四根缝)。
- 第一根缝「身份统一」立项:`requirements/identity-unification.md` + `plans/2026-07-23-identity-unification.md`(调研先行,只读子仓、不改)。
- backlog:OQ-1/OQ-3 移入已解决,身份统一进 Ready(P0)。index 激活 `requirements/` `plans/`。
- 下一步:身份统一 plan 第 1 步——读两侧身份模型(Server 用户体系 + 平台人员注册表)。

## 产品定义讨论 → 毕业成 owner 文档

- 多轮讨论(捕获在 `discussions/2026-07-23-product-definition.md`)确定产品:**企业 AI 工作空间**——每个员工一个「统一工作台」,权限隔离的「智能体」从内部沟通识别工作、经人确认成可执行可溯源的行动;管理者得跨项目任务/进展/风险 rollup。
- 参考用户桌面原型 `D:\project\variAI\原型设计`(派 agent 通读),据此校准并采用其真实词汇/结构。
- 关键决策:主角 = 每个员工(非仅管理者);数据源 = 原生 IM 主 + 外部系统只读事实;项目 = AI 跨群识别的聚合(不绑固定群);客户模型 = A(客户改用本 IM);集成深度 = L2。
- **毕业**:写 `design/app-overview.md` + `requirements/mvp.md`;讨论转存档;index 激活 `design/`;backlog P0 换成「圈定 MVP scope + 拆跨仓计划」。
- 待用户:圈定 MVP scope(`requirements/mvp.md` 里标为提议)。

## IM 地基现状探查(近两天首要任务)

用户定近两天首要 = 把基本聊天/群聊/企业通讯录·组织 + 前端 UI/交互做好。派 2 agent 探 Web / Server+Admin,发现**整套基本已建好**(防烟囱:先查现成、别重建):

- **后端 Server**:`modules/organization/`(企业/部门树 `org_unit` parent-child、员工归属 `org_membership` 主/兼任、CRUD/移动/CSV导入/子管理员授权 API、部门群自动同步、App 端通讯录 `/user/directory` + `/directory/units`)+ 聊天/群聊 TSDD 现成完整。默认 `organization.enabled=true`。
- **Admin**:`pages/organization/index.vue`(1020 行)完整组织控制台——部门树/成员归属/CSV导入/默认密码/管理员授权。
- **Web**:单聊·群聊 TSDD 能用;通讯录 `orgreorgcontacts` 已渲染真组织树(接 `user/directory/units` + memberships),非扁平好友。
- **真实差距**:① 接通验证(跑栈+建组织数据,后端空时通讯录静默降级扁平)② Web 建群选人只有好友列表,部门树分支是**死代码**且用**旧 org API**(`/organizations/{org_id}/department`,`dept_id`)——要统一到新模型(`user/directory/units`,`unit_id`)③ 通讯录只读、空/错态需打磨 ④ 聊天债:`editMessage` 未实现、Voice/QRCode TS 错(基线"错误数不增")。
- **不在此两天**:群看板 body / 工作台 / 日程(FeaturePlaceholder 空壳)= 智能体层,属 P1。

## 后台质量审计(回应用户"最担心后台烂七八糟")

派 2 agent 审 `OrgReOrgServer`,存档 `audits/2026-07-23-server-backend-audit.md`。结论:**不是结构性烂——「大而陌生 + 少量具体安全项」**,不需重写、不用换底座。结构好(插件式模块+DI+统一约定+35 测试+低债标记;朋友圈/红包已被砍),模块化好砍(6 个可各删一行)。真风险按序:①密码 `MD5(MD5)` 无盐(高)②提交的密钥(中,违反自家 AGENTS.md)③2 处 SQL/日志卫生 ④Admin 鉴权手动非中间件 ⑤巨型 api.go 增量拆。建议:短的"地基加固"(①–④ 几天)+ 可选瘦身。

## 地基加固:修复后台审计 #1/#3/#4

- 用户选 1/3/4。在 `OrgReOrgServer` 实现:#1 密码 bcrypt + 兼容旧 MD5 + 机会性迁移 + `user.password` 列加宽迁移;#3 删调试打印 + SQL 参数化;#4 `AdminRoleMiddleware` 挂 8 个 manager auth 组。
- 验证:`go build ./...` + `go vet ./...` + `gofmt` 全绿,`pkg/util` 密码单测通过;**认证全流程需连库冒烟(本机无 Docker 未跑)**。明细见 `audits/2026-07-23-server-backend-audit.md` 修复记录。
- 未做:#2 提交的密钥、#5 巨型 api.go 拆分。未提交(等用户)。
- 追加(用户要 DRY):删掉 7 个 manager 文件里被中间件覆盖的冗余 `CheckLoginRole()`;保留全部 `IsSuperAdmin` + workplace/statistics/非 manager 路由检查。独立核验:build+vet 干净、`IsSuperAdmin` 一个没删、8 处中间件在位。

## 好友系统现状核查(存档 audits)

- 回应用户两问:Web 端 UI 组件库(= Semi UI 唯一组件库,主题走自研 `--wk-*` 令牌)+ 好友系统是否已脱离。
- 静态核查 Web + Server,存档 `audits/2026-07-23-friend-system-status.md`。结论:**功能面前后端均已下线且口径一致(全员互通)**——Web 通讯录已是组织架构、`NewFriend`/`FriendAdd` 成死代码;Server `/v1/friend/*` 路由未注册(404)、`GetUserDetail`/目录接口硬编码 `follow=1`。
- **未清干净**:Web 仍暗跑好友 CMD 监听 + 红点请求;Server friend 表仍被注册流程(系统号/文件助手)和 `webhook.go` 在线状态推送活跃使用。
- ⚠️ 业务后果:在线状态推送按 friend 表取订阅人,同事间互不为好友后实际收不到彼此在线状态;若需要该功能应改按组织目录。
- 彻底清除清单见 audits 文档第五节。未动代码,仅记录。

## 智能体身份层建模讨论(定稿)

- 用户立项:每员工一个个人 AI 助手 + 每项目一个项目 agent;确认**不走好友体系**。
- 摸清地基:`modules/robot/` 现成(robot 用户+app_key+事件长轮询+流式);org 三层(user/employee/membership);旧注册白名单链路已死。
- 定稿身份层 = 照抄真人三层同构:user(robot=1) ↔ 智能体档案(平台侧事实源) ↔ assignment(服务于员工/项目)。
- 用户拍板:D1 一实例一账号;D4 平台为主 IM 存投影(与身份统一缝同向);D5 入职自动开通;D2 独立表(技术决策,Claude 定案:不并入 employee)。
- 存档 `discussions/2026-07-23-agent-identity.md`(含定稿模型/生命周期/不做清单)。
- 下一步:展开成 `plans/` 跨仓计划(平台注册表+同步契约+Server 投影+Web 通讯录分区+引擎接入)。

## 智能体 provision 通道立项(路 B)

- 用户问"平台→IM 同步链路现在有吗"→ 核实:**零对接**(身份统一 plan deferred 仅调研;Server 无平台集成代码;openapi 只有 OAuth;robot 无外部开号通道,仅 `insertSystemRobot` 种子)。
- 现成钩子:Server `AddUser` 支持自定义 uid(未暴露 HTTP);平台 `external_ids` 解析器建好未填;robot API 完整。
- 用户拍板走**路 B**:不等完整身份统一缝,把"平台→IM provision 通道"作为缝的第一个落地切片先做(契约/事实源/投影模式与完整缝同向,后续真人 SSO 复用)。
- 建 `plans/2026-07-23-agent-provisioning.md`(draft):契约先行(provision/deactivate/query + 入职回环)、智能体先走 Shape A(`ag_` 前缀 uid,不预决真人侧)、服务间凭证鉴权、Server 新模块投影表、平台注册表+引擎最小接入、合成冒烟验收。
- backlog:新增 P1「智能体 provision 通道」(plan-first)。
- 下一步:Execution 第 1 步——契约草案定稿并落 `system-baseline.md` 受保护区。

## provision 契约定稿(Execution 1 ✅)

- 定稿前核实三事实:①robot 鉴权的 app_key 属 **app 表**(robot.app_id→app.app_key),provision 需连带建 app 行,决定**每实例独立 app 行**(凭证隔离);②`AddUser` 不设 robot/category,需扩展;③organization 模块**无员工建档事件**,B1 回环需新增 `organization.employee.created`。
- 契约落 `architecture/system-baseline.md` 新节「智能体 provision」(受保护):A1 provision(幂等,Shape A:platform_id=uid=robot_id,ag_ 前缀)/ A2 deactivate(会话保留,project 默认退群)/ A3 query(不回 app_key)+ B1 入职回环(Server 事件→平台建档→回调 A1);双向 `X-Service-Token` 服务间鉴权;引擎数据面复用既有 robot API。
- plan 标 in progress(Execution 1 ✅)。下一步:Execution 2(Server 新模块 agent)或 Execution 3(平台注册表),两者可并行,各在子仓 harness 内做。

## 命名更正:数字员工 → 智能体(Agent)

- 用户觉得"数字员工"不对,讨论后拍板改「智能体/Agent」。三理由:与 D2 决策语义打架(不是 employee)、撞 RPA 行业黑话(暗示替代人)、过度拟人(责任应在人)。
- 趁子仓未动工零成本换名:app-overview(加更名注)、mvp、system-baseline 契约(路由 `/v1/agents`、前缀 `ag_`、`category='agent'`、`agent_no` A0001 段)、plan/discussion 两文件改名(`agent-provisioning` / `agent-identity`)、backlog、本 log 路径引用。product-definition 存档保留原词+后记。核验:全 docs 无残留(仅存档保留)。
- 代码约定定案:模块 `modules/agent`。个人实例显示名仍可叫"我的助理"。

## 数据面改判:删除 robot 模块,agent 管道自建于集成层地基

- 用户追问"删掉开源机器人层再做是不是更简单"→ 重算账后**改判**(推翻同日早前"复用 robot API"的推荐):
  - 核实:`modules/robot`(1518 行)全服务仅 bootstrap 一行引用;管道地基(AddMessagesListener / IMStreamStart/End)在 base/WuKongIM 集成层,不属于 robot 模块;robot 的逐实例长轮询+app_key(绑 app 表)是"少数第三方 bot"模型,不适配每员工一 agent 的舰队规模;Web 端对 robot 仅透传、无 UI 渲染。
  - 契约修订(system-baseline):A1 出参去掉 robot_id/app_key(无逐实例凭证);新增 **A4 出站端点**(messages/stream/typing,薄包集成层)+ **B2 入站单 webhook**(`<平台>/agents/events`,event_id 幂等,取代长轮询);新增「数据面决策」节。
  - plan 同步:Execution 2/3 改为自建管道;新增 Execution 6(删 robot 模块,删前核对 insertSystemRobot 无消费方);Closure gate 加"robot 已删且 build 过"。
  - `user.robot=1` 字段保留(属 user 表,非 robot 模块)。

## Execution 2 完成:Server 侧 modules/agent 落地

- 3 路并行探查(模块注册/配置、消息管道、用户群组织事件接缝)后实现。交付:新模块 `modules/agent`(A1-A3 provision 幂等/A4 出站/B2 入站 Redis 队列推送/B1 入职回环监听)+ group `JoinGroup/LeaveGroup` + organization 两建档路径发 `organization.employee.created` 事件 + `user.CategoryAgent` + `Platform{APIURL,ServiceToken}` 配置 + registry 注册。
- 关键实现决策:派生动作先于投影行落库(重试整体重放)、欢迎消息确定性 client_msg_no 防重、B1 重试借事件总线"不 commit=60s 兜底重投"、B2 保序至少一次(5xx 中断/4xx 出队)、群 @ 过滤支持多 @(优于 robot 单 @)。
- 验证:build+vet 全绿,agent 单测 5/5 通过(含 gin 路由树防 panic 锁),gofmt 干净。**连库冒烟未跑(本机无 Docker)**。未提交 git(等用户)。
- 未动 modules/robot(Execution 6 数据面跑通后删)。下一步:Execution 3 平台侧(OrgReOrg 仓:注册表+B1 接口+/agents/events 接收+最小回复回环)。

## Execution 3 完成:平台侧 orgreorg_im 连接器落地

- 2 路并行探查 OrgReOrg(运行形态/HTTP 能力 + 注册表/agent 机制)后实现。关键发现:平台无 web 框架,HTTP 全是 stdlib 裸壳(wecom_callback/matrix_appservice 先例);ADR-086 已是"一进程服务全 bot 舰队";register_person 一键三步;external_ids 建好未填。
- 交付:`framework/connectors/orgreorg_im/`(client=Server A1/A4 调用方、registry=平台侧智能体事实源 gitignored、service=B1/B2 纯逻辑、http_server=stdlib 壳)+ `scripts/orgreorg_im_server.py`(18130)。B1 幂等(platform_id=ag_+员工uid 确定性)、B2 event_id 去重+最小回复、失败语义与 Server 侧重试机制对齐(5xx 重投/4xx 终态)。
- 循仓内约定:复用 matrix.client transport 与 ledger_io 原子写(shared_helper lint 从违规 1 → 0);顺手把 ledger_io 的 Unix-only fcntl 改条件导入(非 Unix 锁退化,21 测试全过)。
- 验证:37 测试 OK(新 16+ledger 21),4 门禁 lint 绿。完整 ci_check 需 Linux。未提交 git(等用户;该仓自身有 worktree+PR+ci_check 纪律,提交时建议按其流程走)。
- 明确不含:external_ids 回填(需 person↔IM 员工映射=身份统一缝主体)、大脑接入(最小回复先打通)。
- 至此 Execution 1/2/3 齐,剩 4/5 = 部署两端 + 配 token/URL + 合成冒烟(需 Linux 栈环境)。

## 管理端组织群改为只读

- 用户确认群管理里的部门群不应允许手工修改。补齐跨仓契约：Server 后台群列表/封禁列表群项和成员列表响应返回 `category`。
- Admin 对 `department|team` 显示组织架构托管/只读标识，移除禁言、封禁/解禁、移除成员入口；查看成员、聊天记录、黑名单和发消息保持可用。
- Server 原有写接口拒绝逻辑保留为安全边界；组织管理页仍是名称和成员关系的事实源。
- 验证：Server 精准回归测试、`go build ./...`、`go vet ./...` 通过；Admin `pnpm lint`、`pnpm build` 通过。`go test ./modules/group` 的既有路由重复注册 panic 另记为基线问题。

## 修正企业通讯录部门人数口径

- 复核真实数据：`devz-01` 主归属测试部门、兼任研发部，`devz-02` 主归属研发部；因此研发部正确人数为 2。
- 根因是 Web 全局联系人同步统一排除了当前登录员工，组织树复用了该列表，导致本人作为兼任成员时从研发部消失；Server 目录接口和 Admin 组织管理数据均正确。
- Web 为组织架构增加包含本人的独立目录数据，普通联系人场景继续排除本人；部门成员仍按全部有效主/兼任归属聚合。
- 验证：目录口径测试通过，Web 生产构建通过；TypeScript 全量检查仍有 7 个既有 QRCode、Voice、`editMessage` 基线错误，本次未新增。
